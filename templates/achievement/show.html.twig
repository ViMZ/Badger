{% extends 'base.html.twig' %}

{% block title %}Achievement{% endblock %}

{% block body %}
    <h1>Achievement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ achievement.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ achievement.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ achievement.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('achievement_index') }}">back to list</a>

    <a href="{{ path('achievement_edit', {'id': achievement.id}) }}">edit</a>

    {{ include('achievement/_delete_form.html.twig') }}
{% endblock %}
