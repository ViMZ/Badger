{% extends 'base.html.twig' %}

{% block title %}Achievement
{% endblock %}

{% block body %}
	<h1>Achievement</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ achievement.id }}</td>
			</tr>
			<tr>
				<th>Name</th>
				<td>{{ achievement.name }}</td>
			</tr>
			<tr>
				<th>Description</th>
				<td>{{ achievement.description }}</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('achievement_list') }}">Retour à la liste</a>
	<a href="{{ path('achievement_steps', {'id': achivement.id}) }}">Editer les étapes</a>
	<a href="{{ path('achievement_edit', {'id': achievement.id}) }}">Editer le badge</a>

	{{ include('achievement/_delete_form.html.twig') }}
{% endblock %}
