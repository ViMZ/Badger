<form method="post" action="{{ path('achievement_delete', {'id': achievement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ achievement.id) }}">
	<button type="submit" class="text-amber-600 dark:text-amber-500 hover:underline">Supprimer</button>
</form>
