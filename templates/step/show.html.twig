{% extends 'base.html.twig' %}

{% block title %}Etape
{% endblock %}

{% block body %}
	<h1>Etape</h1>
	<a href="{{ path('achievement_steps', {id: step.achievement.id}) }}">Retour à la liste</a>
	<table class="table">
		<tbody></tbody>
	</table>

	{{ include('step/_delete_form.html.twig') }}
{% endblock %}
