{% extends 'base.html.twig' %}

{% block title %}Badge
{% endblock %}

{% block main %}
	<h1>Badge</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ badge.id }}</td>
			</tr>
			<tr>
				<th>Name</th>
				<td>{{ badge.name }}</td>
			</tr>
			<tr>
				<th>Description</th>
				<td>{{ badge.description }}</td>
			</tr>
			<tr>
				<th>Step</th>
				<td>{{ badge.step }}</td>
			</tr>
		</tbody>
	</table>

	<a class="rounded-md border border-2-amber-600 p-3" href="{{ path('achievement_list') }}">Retour à la liste</a>

	<a href="{{ path('badge_edit', {'id': badge.id}) }}">Editer</a>

	{{ include('badge/_delete_form.html.twig') }}
{% endblock %}
