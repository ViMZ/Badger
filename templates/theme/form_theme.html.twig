{% use 'tailwind_2_layout.html.twig' %}

{% block form_row %}
    {% set row_class = row_class|default('mb-6') %}
    {{ parent() }}
{% endblock form_row %}

{% block form_label %}
    {% set label_class = label_class|default('block mb-2 text-sm font-medium text-gray-900') %}
    {{ parent() }}
{% endblock form_label %}

{% block form_widget %}
    {% set widget_class = widget_class|default('bg-white border border-gray-300 text-gray-900 text-sm rounded-lg focus:ring-amber-500 focus:border-amber-500 block w-full p-2.5') %}
    {{ parent() }}
{% endblock %}

{%- block textarea_widget -%}
    {% set widget_class = widget_class|default('bg-white border border-gray-300 text-gray-900 text-sm rounded-lg focus:ring-amber-500 focus:border-amber-500 block w-full p-2.5') %}
    {{ parent() }}
{%- endblock textarea_widget -%}

{%- block number_widget -%}
    {% set widget_class = widget_class|default('bg-white border border-gray-300 text-gray-900 text-sm rounded-lg focus:ring-amber-500 focus:border-amber-500 block w-full p-2.5') %}
    {{ parent() }}
{%- endblock number_widget -%}

{%- block integer_widget -%}
    {% set widget_class = widget_class|default('bg-white border border-gray-300 text-gray-900 text-sm rounded-lg focus:ring-amber-500 focus:border-amber-500 block w-full p-2.5') %}
    {{ parent() }}
{%- endblock integer_widget -%}

{%- block choice_widget_collapsed -%}
    {% set widget_class = widget_class|default('bg-white border border-gray-300 text-gray-900 text-sm rounded-lg focus:ring-amber-500 focus:border-amber-500 block w-full p-2.5') %}
    {{ parent() }}
{%- endblock choice_widget_collapsed -%}

{%- block password_widget -%}
    {% set widget_class = widget_class|default('bg-white border border-gray-300 text-gray-900 text-sm rounded-lg focus:ring-amber-500 focus:border-amber-500 block w-full p-2.5') %}
    {{ parent() }}
{%- endblock password_widget -%}